# Project Memory — Overview

> Single source of truth map for this project's memory bank. Read this first,
> then open only the file you need. The bank describes what the code does and how
> we build it — in prose, tables, and signatures-as-text, never pasted source code.

## How to use

Read the transient state first — `progress.md` (what works / what's left) and
`active-context.md` (what is being worked on right now) — then the durable docs
in this order to understand or rebuild the system bottom-up:

- `architecture/` — module/file layout, layering, dependencies, build + config + env. Read first: the shape to scaffold.
- `data/`         — data model, schema, state, formats, invariants, constants. Read next: the shapes the code moves.
- `interface/`    — public contracts: API, CLI, signatures, events, config keys. Read to know what each unit must expose.
- `behavior/`     — what each unit must do: logic, algorithms, flow, edge cases. Read to fill in the logic.
- `rule/`         — conventions, procedures, how-to ("how we do X"). Read to follow project practice.

Trust each durable statement by its confidence marker: 🟢 `confirmed` (cited from
code), 🟡 `inferred` (deduced — verify against source), 🔴 `gap` (unresolved —
needs a human before you rely on it).

## Map

<!-- BEGIN:MAP (auto-generated by memory_docs.py overview - do not edit by hand) -->
| Category | File | Purpose | Status | Keywords |
|----------|------|---------|--------|----------|
| architecture | [architecture/repo-layout.md](architecture/repo-layout.md) | The three-layer source tree, the shape of one module, and where build entries, scripts and hooks live. | active | application, middleware, driver, bsp, usb_cdc, coredump, protocol, command, workspace, 0xF001, include, src, module directory |
| architecture | [architecture/layering-and-dependencies.md](architecture/layering-and-dependencies.md) | The call direction between layers, the component dependency graph, and why there are two separate error code spaces. | active | REQUIRES, PRIV_REQUIRES, layering, dependency direction, callback, fw_err_t, bsp_err_t, usb_cdc_err_t, ota_err_t, storage_err_t, coredump_err_t, command_busy_cb_t, cfg_store_t, cfg_load_cb_t, cfg_save_cb_t, mapped driver, from_storage_err |
| architecture | [architecture/build-and-toolchain.md](architecture/build-and-toolchain.md) | How the 0xF001 image is configured and compiled, including the main-less ESP-IDF build and the warning policy. | active | EXTRA_COMPONENT_DIRS, COMPONENTS, house_warnings, PROJECT_VER, CMAKE_CONFIGURE_DEPENDS, file(READ), sdkconfig.defaults, idf.py, Werror, esp32s3, fw_config.h, FW_FEATURE_USB_COMMAND, FW_FEATURE_UPDATER, feature switch |
| architecture | [architecture/ci-pipeline.md](architecture/ci-pipeline.md) | What GitHub Actions runs on a push and on a tag, in which container, and which gates can stop a release. | inferred | GitHub Actions, ci.yml, release.yml, espressif/idf:v6.1, ctest, clang-format, clang-tidy, cppcheck, gitleaks, ASan, UBSan, gh release create, SHA256SUMS, app_elf_sha256, dump-to-release index |
| data | [data/error-code-model.md](data/error-code-model.md) | The two status enums the firmware uses, their numeric ranges, and where a vendor code is converted. | active | fw_err_t, bsp_err_t, FW_OK, BSP_OK, FW_ERR_PARAM, FW_ERR_STATE, from_esp_err, esp_err_t |
| data | [data/storage-record.md](data/storage-record.md) | The single versioned, CRC-protected record the device persists, its field layout, and the invariants a reader must enforce. | active | cfg_record_t, CFG_RECORD_VERSION, crc32, manifest_url, last_ok_fw_version, check_interval_ms, boot_fail_count, record_validate, nvs blob, settings record |
| data | [data/flash-and-partitions.md](data/flash-and-partitions.md) | The 16 MB partition table, why the two app slots hold two different applications, the three small data partitions, and the constraints a change must respect. | active | partitions.csv, app_updater, app_firmware, cfg_factory, cfg_setting, coredump, CONFIG_ESP_COREDUMP_FLASH_NO_OVERWRITE, ota_0, ota_1, otadata, nvs, phy_init, rollback, CONFIG_ESPTOOLPY_FLASHSIZE_16MB, CONFIG_ESP_COREDUMP_ENABLE_TO_FLASH |
| data | [data/usb-frame-format.md](data/usb-frame-format.md) | The wire format of the USB command channel, its constants, and what those constants cost in RAM. | active | PROTOCOL_HDR_REQ, PROTOCOL_HDR_RSP, PROTOCOL_MAX_DATA, PROTOCOL_MAX_FRAME, round4, CRC32, protocol_status_t, PROTOCOL_UPG_CHUNK_MIN, PROTOCOL_UPG_CHUNK_CAP, little-endian |
| interface | [interface/fw-status-api.md](interface/fw-status-api.md) | The contract of the project-wide status type and its name lookup. | active | fw.h, fw_config.h, fw_err_t, fw_err_str, fw_crc32_le, FW_OK, FW_ERR_IO, fw_err_t codes, FW_FEATURE_USB_COMMAND, FW_FEATURE_UPDATER, feature switch, crc32 |
| interface | [interface/bsp-api.md](interface/bsp-api.md) | The board contract - resolve the board description, claim its pins, drive the status LED. | active | bsp.h, bsp_init, bsp_deinit, bsp_board_get, bsp_led_status_set, bsp_restart, bsp_mac_get, bsp_mac_kind_t, BSP_MAC_WIFI, BSP_MAC_BLE, BSP_MAC_LEN, bsp_err_str, bsp_board_t, bsp_cfg_t, BSP_GPIO_NONE, bsp_priv.h, bsp_port_pins_t, bsp_port_pins_get, bsp_esp32s3.c, USBPHY_DP_NUM, U0TXD_GPIO_NUM, soc/usb_pins.h, soc/uart_pins.h, IDF_TARGET, port layer |
| interface | [interface/storage-api.md](interface/storage-api.md) | The contract for putting one opaque blob in NVS and reading it back, with no opinion about what is in it. | active | storage.h, storage_err_t, storage_err_str, storage_init, storage_deinit, storage_blob_load, storage_blob_save, storage_cfg_default, storage_t, storage_cfg_t, STORAGE_BLOB_MAX, nvs_get_blob, nvs_set_blob, nvs_flash_init, driver layer, from_storage_err, wear guard |
| interface | [interface/ota-http-api.md](interface/ota-http-api.md) | The contract for downloading a firmware image over HTTPS and receiving it chunk by chunk. | active | ota_http.h, ota_http_init, ota_http_deinit, ota_http_fetch, ota_http_progress_get, ota_http_cfg_default, ota_http_chunk_cb_t, OTA_HTTP_CHUNK_BYTES |
| interface | [interface/updater-api.md](interface/updater-api.md) | The contract of the update cycle - lifecycle, the stepped state machine, and the state callback. | active | updater.h, updater_init, updater_start, updater_stop, updater_deinit, updater_step, updater_state_get, updater_state_str, updater_cfg_default, updater_state_t, updater_state_cb_t, UPDATER_FAIL_LIMIT |
| interface | [interface/tool-esp-cli.md](interface/tool-esp-cli.md) | The command surface of the repo's single developer entry point, what each command expands to, and how it picks the ESP-IDF checkout and the product workspace. | active | tool-esp.py, .env.esp, IDF_PATH, WORKSPACE, IDF_PYTHON_ENV_PATH, MSYSTEM, build, flash, monitor, erase-flash, size, clean, fullclean, menuconfig, merge, format, test, analyse, --port, --workspace, -w, --check, UNITY_DIR, port detection, workspace resolution, resolve_workspace, workspace_refusal, ENV_TEMPLATE, env_value |
| interface | [interface/tool-release-cli.md](interface/tool-release-cli.md) | The contract of the release script - what it refuses, what it changes, and the order it does things in. | active | tool-release.py, --dry-run, --summary, --yes, chore(release), release branch, annotated tag, preflight |
| interface | [interface/protocol-api.md](interface/protocol-api.md) | The contract for parsing USB request frames, building response frames, and looking an opcode up. | active | protocol.h, protocol_parser_feed, protocol_parser_reset, protocol_rsp_build, protocol_cmd_lookup, protocol_status_str, protocol_round4, protocol_parser_t, protocol_req_t, protocol_cmd_info_t |
| interface | [interface/usb-cdc-api.md](interface/usb-cdc-api.md) | The driver contract for the CDC-ACM byte pipe, its VID/PID, and the PHY it takes from USB-Serial-JTAG. | active | usb_cdc.h, usb_cdc_init, usb_cdc_deinit, usb_cdc_write, usb_cdc_is_mounted, usb_cdc_err_str, usb_cdc_err_t, USB_CDC_VID, USB_CDC_PID, esp_tinyusb, TinyUSB, internal PHY |
| interface | [interface/command-map.md](interface/command-map.md) | Every opcode the protocol defines, which thirteen this build serves, and why each of the rest answers -7. | active | command map, opcode, PROTOCOL_CMD_SERVED, PROTOCOL_CMD_UNSUPPORTED, PROTOCOL_ERR_BAD_CMD, PROTOCOL_ERR_UNSUPPORTED, RESTART_APP, PING, BOOT_SLOT, VERSION, WIFI_MAC, BLE_MAC, UPG_BEGIN, UPG_WRITE, UPG_END, DUMP_INFO, DUMP_READ, DUMP_ERASE, retired item |
| interface | [interface/tool-usb-cli.md](interface/tool-usb-cli.md) | The command surface of the host-side USB tool and what each subcommand puts on the wire. | active | tool-usb.py, selftest, ping, version, boot-slot, restart, upgrade, dump, --arm, --chunk, --target, --bytes, --keep, --timeout, pyserial, VID PID detection, esp-coredump |
| interface | [interface/cfg-api.md](interface/cfg-api.md) | The contract for reading and writing the device's settings, one validated accessor pair per setting, with persistence supplied by the caller as an adapter. | active | cfg.h, cfg_t, cfg_store_t, cfg_record_t, cfg_load_cb_t, cfg_save_cb_t, cfg_init, cfg_deinit, cfg_save, cfg_defaults_set, cfg_record_default, cfg_manifest_url_get, cfg_manifest_url_set, cfg_last_ok_fw_version_get, cfg_last_ok_fw_version_set, cfg_check_interval_ms_get, cfg_check_interval_ms_set, cfg_boot_fail_count_get, cfg_boot_fail_count_set, CFG_URL_MAX, CFG_VERSION_MAX, CFG_RECORD_VERSION, CFG_CHECK_INTERVAL_MAX_MS |
| interface | [interface/coredump-api.md](interface/coredump-api.md) | The contract driver/coredump offers for the panic record in flash — whether one is there and sound, its bytes, the panic reason as text, and the erase. | active | coredump_err_t, coredump_state_t, coredump_info_get, coredump_read, coredump_erase, coredump_reason_get, coredump_err_str, COREDUMP_REASON_MAX, COREDUMP_ABSENT, COREDUMP_VALID, COREDUMP_CORRUPT, esp_core_dump_image_get, esp_core_dump_image_check, esp_core_dump_get_panic_reason, coredump_fake |
| interface | [interface/ota-api.md](interface/ota-api.md) | The driver contract for the app slots - what each holds, which one runs, how an image is written into one, and how a fresh boot is confirmed. | active | ota.h, ota_err_t, ota_err_str, ota_session_t, OTA_SESSION_NONE, OTA_SLOT_COUNT, OTA_VERSION_MAX, ota_slot_size_get, ota_slot_version_get, ota_running_slot_get, ota_running_version_get, ota_boot_slot_set, ota_pending_verify_is, ota_mark_valid, ota_session_begin, ota_session_write, ota_session_end, ota_session_abort |
| behavior | [behavior/boot-and-bring-up.md](behavior/boot-and-bring-up.md) | What runs from app_main to the main loop, in what order, and what happens when a step fails. | active | app_main, app_run, print_banner, APP_GIT_COMMIT, bring_up_storage, bring_up_bsp, bring_up_updater, confirm_or_roll_back, report_last_panic, coredump_info_get, coredump_reason_get, on_updater_state, now_ms, APP_TICK_MS |
| behavior | [behavior/update-cycle-fsm.md](behavior/update-cycle-fsm.md) | The updater state machine, its wrap-safe scheduling arithmetic, and its retry budget. | active | updater_step, is_due, enter_state, step_checking, step_downloading, note_failure, UPDATER_FAIL_LIMIT, UPDATER_HORIZON_MS, next_due_ms |
| behavior | [behavior/ota-download-flow.md](behavior/ota-download-flow.md) | How one image fetch runs, from opening the connection to detecting a truncated body. | active | ota_http_fetch, drain_body, esp_http_client_open, fetch_headers, is_complete_data_received, received_bytes, OTA_HTTP_CHUNK_BYTES |
| behavior | [behavior/config-load-and-save.md](behavior/config-load-and-save.md) | How the settings record is validated on read, how a write avoids wearing the flash out, and which module does which half. | active | cfg_init, cfg_save, cfg_defaults_set, record_validate, record_crc, cfg_store_load, cfg_store_save, storage_blob_load, storage_blob_save, read-compare-write, nvs_set_blob, nvs_commit, esp_rom_crc32_le |
| behavior | [behavior/usb-host-flow.md](behavior/usb-host-flow.md) | How a PC drives a 0xF001 unit over USB end to end, what each step proves, and which handlers hold the channel. | active | usb host flow, PING, VERSION, BOOT_SLOT, UPG_BEGIN, UPG_WRITE, UPG_END, RESTART_APP, tool-usb.py, chunk_max, blocking, time budget |
| behavior | [behavior/usb-command-dispatch.md](behavior/usb-command-dispatch.md) | The order the dispatcher decides things in, and what each served handler does. | active | command_on_frame, command_init, command_deinit, serve, handle_restart_app, handle_set_boot_slot, handle_get_version, handle_get_boot_slot, handle_get_mac, command_dump_info, command_dump_read, command_dump_erase, PAYLOAD_MAX, echo, PROTOCOL_DUMP_CHUNK_MAX, command_reply_cb_t, command_busy_cb_t, COMMAND_RESTART_GRACE_MS |
| behavior | [behavior/usb-upgrade-session.md](behavior/usb-upgrade-session.md) | The chunked image transfer over USB - what is checked before anything is erased, the chunk rules, and how a session ends. | active | command_upgrade_begin, command_upgrade_write, command_upgrade_end, command_upgrade_t, chunk_max, img_crc32, ota_session_begin, ota_session_write, ota_session_end, ota_session_abort, ota_session_t, no abort opcode |
| behavior | [behavior/coredump-capture-and-readout.md](behavior/coredump-capture-and-readout.md) | When a panic actually produces a core dump and when it does not, what the stored bytes are, how they leave the unit over USB, and why the .elf that decodes them must be the exact build that crashed. | active | core dump, panic, esp_core_dump_write, panic_restart, PANIC_EXCEPTION, DUMP_INFO, DUMP_READ, DUMP_ERASE, report_last_panic, tool-usb.py dump, esp-coredump, info_corefile, core-format raw, app_elf_sha256, FLASH_NO_OVERWRITE, s_dumping_core |
| rule | [rule/coding-standard-source.md](rule/coding-standard-source.md) | How to resolve a rule ID cited in this repo's comments, and what outranks what. | active | R-RPO, R-LAY, R-ERR, R-LFC, R-CFG, R-BLD, R-VER, R-FMT, embedded-spec, emb-dtbao, spec_doc, spec-verify |
| rule | [rule/formatting-and-hooks.md](rule/formatting-and-hooks.md) | How code layout is decided and enforced, and how to install the hook that enforces it. | active | clang-format, pre-commit, core.hooksPath, InsertBraces, BreakBeforeBraces, format --check |
| rule | [rule/testing.md](rule/testing.md) | Where a test lives, how to run it without a board, and how to tell a test that passes from a test that works. | active | Unity, ctest, test/host, runner.c, UNITY_DIR, host_tests, esp_log stub, mutation, fake, stub, ota_fake, bsp_fake, nvs_fake, coredump_fake, driver fake, 83 tests |
| rule | [rule/static-analysis.md](rule/static-analysis.md) | Which analysers run, what is switched off and why, and how to disposition a finding. | active | clang-tidy, cppcheck, gitleaks, ASan, UBSan, sanitizer, NOLINTNEXTLINE, SANITIZE, analyse |
| rule | [rule/versioning-and-release.md](rule/versioning-and-release.md) | Where the version number lives, what bumps it, and the order of a release. | active | VERSION, PROJECT_VER, CHANGELOG.md, docs/CHANGELOG, SemVer, Unreleased, esp_app_get_description |
| rule | [rule/commit-messages.md](rule/commit-messages.md) | The shape of a commit message here, and the attribution trailer that must never appear in one. | active | Conventional Commits, commit message, trailer, Co-Authored-By, attribution, scope, subject, body |
| rule | [rule/known-deviations.md](rule/known-deviations.md) | Every place this repo departs from the house standard on purpose, plus the unfinished work that must not ship. | active | SPEC-DEVIATION, TODO, R-VER-08, R-RPO-06, R-RPO-01, R-LAY-01, R-LAY-04, gap, self-test, migration, usb, console UART0, PROTOCOL_ERR_UNSUPPORTED, PING ceiling, esp_log exception, esp_http_client exception, mapped driver |
| rule | [rule/layer-boundaries.md](rule/layer-boundaries.md) | The rule that keeps the vendor SDK out of middleware and application code, its two named exceptions, and the grep that enforces it. | active | R-LAY-01, R-LAY-03, R-LAY-04, mapped driver, wrapper, esp_ota_begin, esp_log, esp_http_client, esp_rom_crc32_le, nvs, layering, portability line, driver fake |
<!-- END:MAP -->

## Conventions

Durable docs follow semantic `kebab-case.md` naming inside their category folder,
with frontmatter (`title`, `category`, `purpose`, `status`, `updated`, `source`,
`confidence`, `keywords`, optional `order`). Look a doc up by theme in `Purpose`
or by exact name in `Keywords`. Reading order comes from the category order above
then the `order` field, not the file name. One concept per file, under ~300
lines, front-load a one-line summary, cite the source path (with `:line` where it
helps), link instead of repeating. The two state files carry only `title` +
`updated` and are exempt from category naming. See the `skill-memory-ai` reference
`memory-doc-rules.md` for the full rules.
